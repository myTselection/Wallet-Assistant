{
  "name": "WalletAssistant",
  "version": "1.0.0",
  "description": "Wallet Assistant Lovelace card",
  "scripts": {
    "build": "esbuild src/walletassistant-main.js --bundle --minify --outfile=dist/walletassistant-card.js --loader:.css=text --format=esm"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  },
  "dependencies": {
    "jsbarcode": "^3.11.6",
    "qrcode": "^1.5.4"
  }
}
